// game: Core game loop and rendering logic go here — separate from React components.
// Handles the SignalR connection, processes server state updates, and drives the game canvas/display.
// Keep this framework-agnostic so it can be tested independently of React.
